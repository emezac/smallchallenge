PUBLIC_ORIGIN=https://gateway.example.invalid
AUDIT_KEY=
AUDIT_KEY_ID=v1
AUDIT_PREVIOUS_KEYS_JSON={}
SECRET_KEY_BASE=
# Authentication must be confirmed with AIFindr before public deployment.
OAUTH_ISSUER=
# Prefer the issuer's trusted JWKS snapshot; configure exactly one key source.
OAUTH_JWKS_FILE=
OAUTH_PUBLIC_KEY_FILE=
OAUTH_REVOKED_JTIS=
BUNDLE_PATH=vendor/bundle
